import InputReader, {dateFormat} from "./InputReader"


function readInputs() {
  let dateFrom = InputReader.readDate("Entrer une date de debut (DD-MM-YYYY): ");
  
  let dateTo = InputReader.readDate("Entrer une date de fin (DD-MM-YYYY): ");
}

readInputs();